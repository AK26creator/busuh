# Supabase Connection Details
# Replace these with your actual Supabase project values
# You can find these in Project Settings > API

VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# Note: VITE_ prefix is required for Vite projects to access 
# variables in the client-side code via import.meta.env
# Maps are now FREE with Leaflet + OpenStreetMap (no API key needed!)
